import "package:flutter/material.dart";

class PricesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(
        child: Center(child: const Text("Prices")),
      ),
    );
  }
}
